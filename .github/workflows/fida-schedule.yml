name: FIDA 자동 실행

on:
  schedule:
    - cron: '0 21 * * 1-5'   # 화~토 06:00 KST (UTC 기준 월~금 21:00)
  workflow_dispatch:           # GitHub UI에서 수동 트리거 가능

jobs:
  run-fida:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Gemini quota 상태 복원
        uses: actions/cache/restore@v4
        with:
          path: .fida-state
          key: fida-gemini-quota-${{ github.run_id }}
          restore-keys: |
            fida-gemini-quota-

      - name: service-account.json 생성
        run: |
          mkdir -p secrets
          mkdir -p .fida-state
          echo "${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON_B64 }}" | base64 -d > secrets/service-account.json

      - name: .env 파일 생성
        run: |
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
          echo "FANDING_EMAIL=${{ secrets.FANDING_EMAIL }}" >> .env
          echo "FANDING_PASSWORD=${{ secrets.FANDING_PASSWORD }}" >> .env
          echo "GOOGLE_SHEETS_SPREADSHEET_ID=${{ secrets.GOOGLE_SHEETS_SPREADSHEET_ID }}" >> .env
          echo "GOOGLE_SERVICE_ACCOUNT_JSON_PATH=/secrets/service-account.json" >> .env
          echo "TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}" >> .env
          echo "TELEGRAM_CHAT_ID=${{ secrets.TELEGRAM_CHAT_ID }}" >> .env
          echo "INTERNAL_API_TOKEN=${{ secrets.INTERNAL_API_TOKEN }}" >> .env
          echo "GEMINI_QUOTA_USAGE_PATH=/state/gemini-quota-usage.json" >> .env

      - name: FIDA 실행
        run: |
          docker compose run --rm \
            -e SPRING_PROFILES_ACTIVE=job \
            -e SPRING_MAIN_WEB_APPLICATION_TYPE=none \
            fida

      - name: 정리
        if: always()
        run: docker compose down

      - name: Gemini quota 상태 저장
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .fida-state
          key: fida-gemini-quota-${{ github.run_id }}
